# Network Health Intent

This file records the intended diagnostic surface for the inventory.

## Hosts
- `host_a`: OpenSSH=openssh, zone=lab-edge-a, scopes=gateway, shard, repo-sync, diagnostics
- `host_b`: OpenSSH=openssh, zone=lab-edge-b, scopes=shard, repo-sync, diagnostics
